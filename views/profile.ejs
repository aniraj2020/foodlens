<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Your Profile - FoodLens</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  
  <!-- Materialize and Icons -->
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/style.css" />
</head>

<body class="home-body">

  <!-- App Bar -->
  <div class="app-bar">
    <div class="app-bar-left">
      <i class="material-icons left">code</i>
      <a href="https://github.com/aniraj2020/FoodLens" target="_blank">GitHub Repo</a>
    </div>
    <div class="app-bar-center">
      Welcome to FoodLens, <%= user.username %>!
    </div>
    <div class="app-bar-right">
      <a href="/logout" class="btn red">Logout</a>
    </div>
  </div>

  <!-- Profile Card -->
  <div class="info-section center-align" style="max-width: 600px; margin: 10rem auto;">
    <h4><i class="material-icons left">account_circle</i> Profile</h4>
    <div class="card-panel z-depth-1">
      <p><strong>Username:</strong> <%= user.username %></p>
      <p><strong>Role:</strong> <%= user.role %></p>
      <p><strong>Joined On:</strong> 
        <%= user.CreatedAt ? new Date(user.createdAt).toLocaleString("en-AU", { 
              day: '2-digit', 
              month: 'long', 
              year: 'numeric' 
        }) : "N/A" %>
      </p>
    </div>

    <div style="margin-top: 2rem;">
      <a href="/dashboard" class="btn blue"><i class="material-icons left">home</i>Back to Dashboard</a>
      <a href="/logout" class="btn red"><i class="material-icons left">exit_to_app</i>Logout</a>
    </div>
  </div>

  <!-- Scripts -->
  <script src="/socket.io/socket.io.js"></script>
  <script src="/socket_init.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>
</html>
